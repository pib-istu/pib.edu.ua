import React, {useEffect} from 'react';

const Header = (props) => {
    useEffect(() => {

    });

    return(
        <header className="header">

        </header>
    )
};

export default Header;